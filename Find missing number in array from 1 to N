public static int missingNumber(int A[], int N)
    {
         // Your code goes here
         
         int sum=0;
         for(int i=0;i<N;i++){
             sum+=A[i];
         }
         int ObSum=(N*(N+1))/2;
         return (ObSum-sum);
    }
}
